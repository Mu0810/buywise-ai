/**
 * Marketing re-export of the shared motion primitives, kept so feature code
 * can import from its own folder. See `src/components/motion`.
 */
export { Reveal, StaggerGroup, StaggerItem } from "@/components/motion";
